# Global config and platform provider (GCP/Azure)
# Combined with artifacts.yaml and resources.yaml via helmfile

environment: dev                          # Environment name: dev | staging | preprod | prod
platform:
  provider: GCP                           # GCP or Azure
  gcp:
    secretsProjectId: "<gcp-project-id>"
    storage_configs:
      bucket: "<gcs-bucket-name>"
  azure:
    keyVaultUri: "https://<vault-name>.vault.azure.net/"
    storage_configs:
      container: "<azure-container-name>"
      storage_account: "<azure-storage-account>"

chartBasePath: /path/to/divyam-helm-charts  # Absolute path to the helm charts repo
clusterDomain: ""                           # Leave empty for in-cluster; set for cross-cluster DNS
